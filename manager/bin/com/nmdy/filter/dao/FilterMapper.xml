<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nmdy.filter.dao.FilterMapper" >

    <resultMap
        id="functionResult"
        type="com.nmdy.filter.pojo.Function" >

        <result
            column="id"
            property="id" />

        <result
            column="url"
            property="url" />
    </resultMap>

    <select
        id="isHasAccessToTheURI"
        parameterType="hashmap"
        resultType="int" >
		select count(c.url) cnt from 
			(select role_oper_id from administrator_operrole_rel_zyl where administrator_id=#{userId}) a
			left join role_oper_func_rel_zyl b on a.role_oper_id=b.role_oper_id
			left join func_point_zyl c on b.func_point_id=c.id where c.url=#{requestURI}
    </select>

    <select
        id="getSubFunctions"
        parameterType="hashmap"
        resultMap="functionResult" >
		select c.url requestURI from 
			(select role_oper_id from administrator_operrole_rel_zyl where administrator_id=#{userId}) a
			left join role_oper_func_rel_zyl b on a.role_oper_id=b.role_oper_id
			left join func_point_zyl c on b.func_point_id=c.id where c.parent=(select id from func_point_zyl where url=#{requestURI})
    </select>

</mapper>